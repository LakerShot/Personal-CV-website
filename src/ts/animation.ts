export const initAnimation = () => {
    // gsap.from(".main__info", { x: -1200, duration: 1 })
    // gsap.from(".main__picture-container", { y: -200, duration: 1, opacity: 0 })
    // gsap.from(".portfolio__header", { y: -100 })
}